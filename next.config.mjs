/** @type {import('next').NextConfig} */
const nextConfig = {};

export const i18n = {
  locales: ["default", "it-IT", "en-EN", "it", "en"],
  defaultLocale: "default",
  localeDetection: true,
};

export default nextConfig;
